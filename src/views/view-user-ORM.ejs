<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Oải Cả Chưởng</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <table>
        <tr>
            <th>Id</th>
            <th>firstName</th>
            <th>lastName</th>
            <th>Email</th>
            <th>Addres</th>
        </tr>
        <% for(let i=0; i < userData.length ; i++ ){ %>
            <tr>
                <td>
                    <%= userData[i].id %>
                </td>
                <td>
                    <%= userData[i].firstName %>
                </td>
                <td>
                    <%= userData[i].lastName %>
                </td>
                <td>
                    <%= userData[i].email %>
                </td>
                <td>
                    <a href="/detail/<%= userData[i].id %>">detail</a>
                    <a href="/edit-userORM?id=<%= userData[i].id %>">edit</a>
                    <form action="/delete-UserORM" method="post">
                        <input type="text" name="id" value="<%= userData[i].id %>">
                        <button type="submit">delete</button>
                    </form>
                </td>
            </tr>
            <% } %>
    </table>
</body>

</html>